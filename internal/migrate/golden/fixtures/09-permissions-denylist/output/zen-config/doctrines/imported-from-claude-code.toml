# Generated by `zen migrate claude-code`.
# 1:1 preservation per invariant. Source: <SOURCE>

schema_version = "1.0"
name = "imported-from-claude-code"
inherits_from = "max-scope"
description = "Imported from ~/.claude/settings.json permissions."

[capa_firewall]
allow = [
]
deny = [
  "Write(.env)",
  "Bash(sudo:*)",
  "filesystem.write(/etc/passwd)",
]

[capa_firewall.tiers]
"Bash(sudo:*)" = "low"
"Write(.env)" = "high"
"filesystem.write(/etc/passwd)" = "high"
