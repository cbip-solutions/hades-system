
default_provider:
  name: anthropic
  model: "opus[1m]"

mcp_servers:
  playwright:
    command: npx
    args: ["@playwright/mcp"]
  hades-system:
    transport: http
    url: http://unix/v1/mcpgateway
    socket: /tmp/hades-system.sock

