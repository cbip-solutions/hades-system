package x

// StillValid is a well-formed function before the broken one.
func StillValid() int {
	return 1
}

// Broken has a syntax error mid-body (missing closing brace + dangling expr).
func Broken(x int) int {
	if x >
}

// AlsoValid follows the broken function.
func AlsoValid() string {
	return "ok"
}
