# Strict-mode fixture: typo in section name. Operator wrote
# [doctrine_transvers] instead of [doctrine_transverse]. Strict mode
# treats every key under the typo'd section as unknown. ParseStrict
# MUST reject with a clear error pointing at the first unknown key.
schema_version = "1.0"
doctrine_version = "1.0.0"

[doctrine_transvers]   # typo: missing trailing "e"
no_tech_debt = true
no_stubs = true
