# Built-in flavor fixture: max-scope.toml with [doctrine_transverse]
# section present. ParseStrict must accept this when caller passes
# AllowTransverseDeclaration=true (Phase D embedded built-in path) and
# reject it when caller passes AllowTransverseDeclaration=false (Phase G
# user/override path) per inv-zen-135.
#
# DEVIATION FROM PLAN: section name is [doctrine_transverse] (underscore)
# not [doctrine.transverse] (dotted) — this matches Phase A's actual
# struct tag `toml:"doctrine_transverse"` on Schema.Transverse. The plan
# was speculative on the section path; Phase A is the source of truth.
schema_version = "1.0"
doctrine_version = "1.0.0"

[doctrine_transverse]
no_tech_debt = true
no_stubs = true
build_final_product = true
no_defer = true
