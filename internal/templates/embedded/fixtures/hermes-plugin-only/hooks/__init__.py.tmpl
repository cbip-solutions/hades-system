"""{{.ProjectName}}.hooks — Hermes lifecycle hook handlers.

Implementations live in:
- session_handlers.py  (on_session_start, on_session_end)
- tool_handlers.py     (pre_tool_call, post_tool_call)

Replace the stubs below with real logic.
"""
