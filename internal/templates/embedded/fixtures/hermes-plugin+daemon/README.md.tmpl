# {{.ProjectName}}

Hermes plugin with a companion Go daemon, scaffolded by zen-swarm.

Doctrine: `{{.Doctrine}}`.

## Quick start

```bash
make build               # compiles bin/{{.ProjectName}}-ctld
bin/{{.ProjectName}}-ctld --help
```
