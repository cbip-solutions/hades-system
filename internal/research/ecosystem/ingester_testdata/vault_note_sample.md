# Hashing notes

I want to use `crypto/sha256.Sum256` for fixed-size hashing in Go.

For matrix solves I'll use `numpy.linalg.solve` (Python).

Frontend uses `react.useState` for component state.

For Rust serialization see `serde::Serialize`.
