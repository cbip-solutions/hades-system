# zen-swarm operator zshrc fixture for Plan 18c Phase F unit tests.
# The migrate tool replaces /zen-swarm:* with /hades:*.
alias zs="run /zen-swarm:start"
alias zsh="run /zen-swarm:handoff"
export PATH="$HOME/.zen/bin:$PATH"
