# .goreleaserignore — Plan 15 Phase D-1
# Excluded from goreleaser snapshot tarballs (parallel to .gitignore but
# scoped to the dist/ packaging step, not git tracking).

# Build outputs (we ship binaries from goreleaser builds, not Makefile dist)
bin/
dist/

# Test fixtures + golden data (large, irrelevant to release tarball)
tests/integration/fixtures/
tests/realworld/fixtures/
tests/compliance/testdata/

# Plugin Python source NOT shipped in Go tarball (separate brew formula
# eventually; currently ships via Hermes config + manual install)
plugin/zen-swarm/bin/zen-event-poster

# Local development artifacts
*.test
*.out
coverage.txt
.DS_Store
